Finished soundbites live in this folder (mp3 / m4a).
Don't add files here by hand — use tools/soundbite.py, which also keeps
soundbites.json in sync. Downloaded full episodes belong in ../episodes/
(git-ignored), not here.
